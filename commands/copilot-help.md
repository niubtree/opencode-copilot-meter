---
description: Show copilot-meter help
---
Detect the operating system and run the appropriate command using the bash tool:
- On Windows: `pwsh -NoProfile -Command "& (Join-Path $HOME '.config/opencode/scripts/copilot-meter/copilot-help.ps1')"`
- On macOS/Linux: `bash ~/.config/opencode/scripts/copilot-meter/copilot-help.sh`

Output the result verbatim with no additional commentary.
