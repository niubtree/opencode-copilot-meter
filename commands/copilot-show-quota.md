---
description: Show remaining Copilot premium interactions
---
Detect the operating system and run the appropriate command using the bash tool:
- On Windows: `pwsh -NoProfile -Command "& (Join-Path $HOME '.config/opencode/scripts/copilot-meter/copilot-show-quota.ps1')"`
- On macOS/Linux: `bash ~/.config/opencode/scripts/copilot-meter/copilot-show-quota.sh`

Output the result verbatim with no additional commentary.
